model:
  default: glm-5-turbo
  provider: custom:aperture
custom_providers:
  - name: aperture
    base_url: http://ai
    key_env: ANTHROPIC_API_KEY
    api_mode: anthropic_messages
terminal:
  backend: local
  timeout: 180
